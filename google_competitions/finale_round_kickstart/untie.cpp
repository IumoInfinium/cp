#include <bits/stdc++.h>

using namespace std;

int main(){
    int testcase;
    cin >> testcase;
    string s;
    for(int t=0; t<testcase; t++){
        cin >> s;
        int n = s.size();
        int curr=0;
        int prev=n-1;
        int next = 1;
        for(curr=0; curr<s.size(); i++){
            if(s[curr] == s[prev])
        }
    }
    return 0;
}
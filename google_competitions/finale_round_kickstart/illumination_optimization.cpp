#include<bits/stdc++.h>
using namespace std;


int helper(int cap, vector<int>& bulbs, vector<int>& dp, int used){
    if(dp.)
}

int main(){
    int testcase, m, r, n;
    cin >> testcase;
    for(int t=0; t < testcase; t++){
        cin >> m >> r >> n;

        vector<int> bulbs;
        for(int i=0; i<n; i++) cin>> bulbs[i];
        vector<int> dp(m+1,0);
        
        int i=0;
        for(i=0; i<n; i++){
            if(dp.)
        }


        cout << "Case #" << t+1 << ": " ;
        if(ans == 0) cout << "IMPOSSIBLE\n";
        else cout << ans;
    }
    return 0;
}